import ErrorScreen from "../special_screens/ErrorScreen";

const Unconnected = () => {
  return <ErrorScreen>Server error. Please try again in a minute.</ErrorScreen>;
};

export default Unconnected;
